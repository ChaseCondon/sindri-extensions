# Aurora UI Icons

Activity bar and toolbar icons for the Sindri Aurora theme. Uses the same runic glyph set as the default Sindri UI icons — the aurora character comes from the theme's violet `#B06CFF` accent being applied to `currentColor` in these monochrome SVGs.

Install the **Aurora Pack** to get the Aurora theme, file icons, and these UI icons together.
