{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/sindri-ide/extensions/manifest.schema.json",
  "title": "Sindri Extension Manifest",
  "description": "Schema for a Sindri extension manifest.json file.",
  "type": "object",
  "required": ["id", "name", "version", "publisher", "description", "categories", "permissions", "engines", "contributes"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Globally unique extension identifier in 'publisher.name' dot-notation. Example: 'sindri.aurora-theme'",
      "pattern": "^[a-z0-9-]+\\.[a-z0-9-]+$"
    },
    "name": {
      "type": "string",
      "description": "Human-readable extension name shown in the marketplace. Example: 'Sindri Aurora'"
    },
    "version": {
      "type": "string",
      "description": "Semantic version of this manifest. Must match a git tag in the extension repository. Example: '1.0.0'",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "publisher": {
      "type": "string",
      "description": "Publisher identifier — matches the prefix of 'id'. Example: 'sindri'"
    },
    "description": {
      "type": "string",
      "description": "One-sentence description shown in the marketplace card."
    },
    "categories": {
      "type": "array",
      "description": "Marketplace browsing facets. An extension may belong to multiple categories.",
      "minItems": 1,
      "items": {
        "type": "string",
        "enum": [
          "Color Theme",
          "File Icon Theme",
          "UI Icon Theme",
          "Language Support",
          "Language Pack",
          "Test & Task Adapter",
          "UI Extension"
        ],
        "description": "Color Theme — UI palette (sindri-dark.json style); File Icon Theme — file tree icons; UI Icon Theme — activity bar / toolbar glyphs; Language Support — Tree-sitter grammar + LSP/DAP adapters via SAP; Language Pack — human-language UI localisation; Test & Task Adapter — test runner / task adapter; UI Extension — general workbench UI additions (requires extension host)"
      }
    },
    "permissions": {
      "type": "array",
      "description": "sindri.* API namespaces this extension may call (ADR-0015 §6). Empty array for data-only extensions (themes, icons). Example: ['sindri.workspace', 'sindri.terminal']",
      "items": {
        "type": "string",
        "pattern": "^sindri\\."
      }
    },
    "engines": {
      "type": "object",
      "description": "Minimum Sindri version this extension is compatible with.",
      "required": ["sindri"],
      "additionalProperties": false,
      "properties": {
        "sindri": {
          "type": "string",
          "description": "Semver range. Use '>=0.1.0' for extensions compatible with all 0.x releases. Example: '>=0.2.0'",
          "examples": [">=0.1.0", ">=1.0.0", "^1.2.0"]
        }
      }
    },
    "contributes": {
      "type": "object",
      "description": "What this extension provides. All fields are optional; include only what applies.",
      "additionalProperties": false,
      "properties": {
        "themes": {
          "type": "array",
          "description": "Colour theme definitions (data-only, no extension host required).",
          "items": {
            "type": "object",
            "required": ["id", "name", "kind", "path"],
            "additionalProperties": false,
            "properties": {
              "id":   { "type": "string", "description": "Unique theme ID. Example: 'my-theme-dark'" },
              "name": { "type": "string", "description": "Display name. Example: 'My Theme Dark'" },
              "kind": { "type": "string", "enum": ["light", "dark"], "description": "Controls which CSS variable set is applied." },
              "path": { "type": "string", "description": "Path to the ThemeDef JSON file, relative to this extension folder. Example: 'aurora-dark.json'" }
            }
          }
        },
        "iconThemes": {
          "type": "array",
          "description": "File icon theme definitions (data-only, no extension host required).",
          "items": {
            "type": "object",
            "required": ["id", "name", "kind", "path"],
            "additionalProperties": false,
            "properties": {
              "id":   { "type": "string" },
              "name": { "type": "string" },
              "kind": { "type": "string", "enum": ["color", "mono"], "description": "'color' — full-colour SVG icons; 'mono' — uses currentColor, respects the theme accent." },
              "path": { "type": "string", "description": "Path to the IconThemeDef JSON file." }
            }
          }
        },
        "lsp": {
          "type": "array",
          "description": "Language Server Protocol adapters (requires extension host).",
          "items": {
            "type": "object",
            "required": ["id", "languageId", "launch"],
            "additionalProperties": false,
            "properties": {
              "id":         { "type": "string" },
              "languageId": { "type": "string", "description": "CodeMirror / Tree-sitter language identifier. Example: 'rust'" },
              "launch": {
                "type": "object",
                "required": ["command"],
                "properties": {
                  "command": { "type": "string" },
                  "args":    { "type": "array", "items": { "type": "string" } }
                }
              }
            }
          }
        },
        "dap": {
          "type": "array",
          "description": "Debug Adapter Protocol adapters (requires extension host).",
          "items": {
            "type": "object",
            "required": ["id", "languageId", "launch"],
            "additionalProperties": false,
            "properties": {
              "id":         { "type": "string" },
              "languageId": { "type": "string" },
              "launch": {
                "type": "object",
                "required": ["command"],
                "properties": {
                  "command": { "type": "string" },
                  "args":    { "type": "array", "items": { "type": "string" } }
                }
              }
            }
          }
        },
        "grammars": {
          "type": "array",
          "description": "Tree-sitter / Lezer grammar files (requires extension host).",
          "items": {
            "type": "object",
            "required": ["languageId", "path"],
            "additionalProperties": false,
            "properties": {
              "languageId": { "type": "string" },
              "path":       { "type": "string" }
            }
          }
        },
        "taskAdapters": {
          "type": "array",
          "description": "Task runner adapters (requires extension host).",
          "items": {
            "type": "object",
            "required": ["id", "languageId"],
            "additionalProperties": false,
            "properties": {
              "id":         { "type": "string" },
              "languageId": { "type": "string" }
            }
          }
        },
        "treeViews": {
          "type": "array",
          "description": "Tree-view panels contributed by this extension (ADR-0026 Tier 1). Each entry declares a panel that the extension host will register when sindri.ui.registerTreeView(id, …) is called from activate().",
          "items": {
            "type": "object",
            "required": ["id", "title"],
            "additionalProperties": false,
            "properties": {
              "id":          { "type": "string", "description": "Tree-view identifier — passed to sindri.ui.registerTreeView. Must be unique within the extension. Example: 'my-ext.my-tree'" },
              "title":       { "type": "string", "description": "Panel title shown in the dock tab. Example: 'My Tree'" },
              "icon":        { "type": "string", "description": "SVG markup for the activity-bar icon. Defaults to a generic list icon." },
              "defaultDock": {
                "type": "string",
                "enum": ["left-top", "left-bottom", "right-top", "right-bottom", "bottom"],
                "description": "Which dock region the panel initially appears in. Defaults to 'left-top'."
              }
            }
          }
        },
        "configuration": {
          "type": "object",
          "description": "Settings schema contributed to the Settings panel (ADR-0021). Keys are setting identifiers.",
          "additionalProperties": {
            "type": "object",
            "required": ["type"],
            "properties": {
              "type":        { "type": "string", "enum": ["string", "boolean", "number", "enum"] },
              "default":     {},
              "description": { "type": "string" },
              "enum":        { "type": "array", "items": { "type": "string" } }
            }
          }
        }
      }
    },
    "main": {
      "type": "string",
      "description": "Entry-point bundle path for code-bearing extensions (requires extension host). Omit entirely for data-only extensions (themes, icons). Example: 'dist/extension.js'"
    }
  }
}
