# Sindri UI Icons

The default Sindri activity bar and toolbar icon set, published as a standalone extension.

SVG sources live in `icons/`. The icon IDs map to tool-window identifiers (`explorer`, `search`, `git`, `debug`, `extensions`, `terminal`, `problems`, `output`, `settings`, and more).

**Status:** Data-only — installs without the extension host.
