# OS
.DS_Store

# Build artifacts — JS bundles and source maps are ephemeral; .sinxt is the committed distributable
**/dist/*
!**/dist/*.sinxt
nightly-dist/

# Bun / Node
node_modules/
bun.lockb

# Rust build artifacts inside extension source trees
**/binary-src/target/
**/wasm-src/target/

# Claude Code — never commit AI tooling metadata
.claude/
CLAUDE.md
HANDOVER.md
memory/
