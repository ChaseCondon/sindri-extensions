// CSV Grid extension — implementation deferred to a future phase (Surface B, ADR-0028).
// Not yet installable. See manifest.json buildable: false.

export async function activate(_context: ExtensionContext): Promise<void> {}
export function deactivate(): void {}
